pipeline {
    agent { docker { image 'python:2.7.13' } }
    stages {
        stage('build'){
            steps{
             sh 'sudo pip install django-jenkins'
            }
        }
        stage('runserver'){
            steps{
                 sh 'python manage.py runserver'
            }
        }
    }
}
